<style>
    form {
        display: flex;
        flex-direction: column;
        align-items: center;
        border: 1px solid gray;
        border-radius: 1rem;
        margin: 1rem;
    }

    label {
        margin: 0.5rem;
    }

    button {
        margin: 0.5rem;
        border: 1px solid green;
        background-color: green;
        border-radius: 1rem;
        padding: 0.5rem 1rem;
    }

    select {
        width: 20rem;
        height: 2rem;
        border-radius: 0.5rem;
        border: 1px solid #ccc;
        padding: 0.5rem;
        margin: 0rem 0rem 1rem;
    }
</style>

<script>
    const createSubmit = () => {};
    // () => {
    //     const selectBook = document.getElementById('select-book');
    //     const bookId = selectBook.value;
    
    //     fetch(location.href=`/livros/${bookId}`, {
    //         method: 'DELETE'
    //     })
    //     .catch(error => console.log(error));
    // }
</script>

<form align="center">
    <label for="select-book">Escolha umas das opções abaixo</label>
    <select name="select-crud" id="select-crud">

        <% for (let option of options ) { %>

            <option value= <%= `${option.host}` %> ><%= option.name %></option>

        <% } %>

    </select>

    <button type="submit" onclick=createSubmit()>Criar</button>
</form>